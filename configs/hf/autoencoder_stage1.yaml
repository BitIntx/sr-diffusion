project:
  name: autoencoder_stage1_hf

model:
  type: autoencoder_kl
  in_channels: 3
  out_channels: 3
  base_channels: 96
  channel_multipliers: [1, 2, 4]
  latent_channels: 16
  num_res_blocks: 2
  norm_groups: 32
